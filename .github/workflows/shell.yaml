on:
  pull_request:
    paths:
      - "task/scripts/*"

name: Shell

jobs:
  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: go-task/setup-task@v2

      - run: |
          task lint:shell-script
