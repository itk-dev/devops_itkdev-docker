
api:
  dashboard: true
  insecure: true
  debug: true

entryPoints:
  http:
    address: ":80"
  https:
    address: ":443"
    http:
      tls:
        {}

providers:
  file:
    directory: /config
  docker:
    endpoint: "tcp://socket-proxy:2375"
    exposedByDefault: false
